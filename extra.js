// const image = document.querySelector('.weather-box img');
            // const temperature = document.querySelector('.weather-box .temperature');
            // const description = document.querySelector('.weather-box .description');
            // const humidity = document.querySelector('.weather-details .humidity span');
            // const wind = document.querySelector('.weather-details .wind span');
            
            // document.querySelectorAll('#clone-info-weather, #clone-info-humidity, #clone-info-wind').forEach(clone => clone.remove());


            // if (cityHide.textContent == city) {
            //     return;
            // } else {

                // const existingClonesWeather = document.querySelectorAll('#clone-info-weather');
                // const existingClonesHumidity = document.querySelectorAll('#clone-info-humidity');
                // const existingClonesWind = document.querySelectorAll('#clone-info-wind');

                // existingClonesWeather.forEach(clone => clone.remove());
                // existingClonesHumidity.forEach(clone => clone.remove());
                // existingClonesWind.forEach(clone => clone.remove());

                // const infoWeather = document.querySelector('.info-weather');
                // const infoHumidity = document.querySelector('.info-humidity');
                // const infoWind = document.querySelector('.info-wind');

                // const elCloneInfoWeather = infoWeather.cloneNode(true);
                // const elCloneInfoHumidity = infoHumidity.cloneNode(true);
                // const elCloneInfoWind = infoWind.cloneNode(true);

                // elCloneInfoWeather.id = 'clone-info-weather';
                // elCloneInfoWeather.classList.add('active-clone');

                // elCloneInfoHumidity.id = 'clone-info-humidity';
                // elCloneInfoHumidity.classList.add('active-clone');

                // elCloneInfoWind.id = 'clone-info-wind';
                // elCloneInfoWind.classList.add('active-clone');

                // infoWeather.insertAdjacentElement("afterend", elCloneInfoWeather);
                // infoHumidity.insertAdjacentElement("afterend", elCloneInfoHumidity);
                // infoWind.insertAdjacentElement("afterend", elCloneInfoWind);

                
//                     setTimeout(() => {
    //                     infoWeather.insertAdjacentElement("afterend", elCloneInfoWeather);
    //                     infoHumidity.insertAdjacentElement("afterend", elCloneInfoHumidity);
    //                     infoWind.insertAdjacentElement("afterend", elCloneInfoWind);
    //                 }, 2200);
    
    //                 const cloneInfoWeather = document.querySelectorAll('.info-weather.active-clone');
    //                 const totalCloneInfoWeather = cloneInfoWeather.length;
    //                 const cloneInfoWeatherFirst = cloneInfoWeather[0];
    
    //                 const cloneInfoHumidity = document.querySelectorAll('.info-humidity.active-clone');
    //                 const cloneInfoHumidityFirst = cloneInfoHumidity[0];
    
    //                 const cloneInfoWind = document.querySelectorAll('.info-wind.active-clone');
    //                 const cloneInfoWindFirst = cloneInfoWind[0];
    
    //                 if (totalCloneInfoWeather > 0) {
    //                     cloneInfoWeatherFirst.classList.remove('active-clone');
    //                     cloneInfoHumidityFirst.classList.remove('active-clone');
    //                     cloneInfoWindFirst.classList.remove('active-clone');
    
    //             setTimeout(() => {
    //             cloneInfoWeatherFirst.remove();
    //             cloneInfoHumidityFirst.remove();
    //             cloneInfoWindFirst.remove();
    //          }, 2200);
    //        }
    //    }
    // })